package config

const (
	//HarvestURL that represents the URL of the Harvest API
	HarvestURL = "https://harvest.greenhouse.io/v1/candidates?"
	// PerPageParam that represents the number of candidates per page shown.
	PerPageParam = "per_page="
	// PageParam that represents the page number of the query.
	PageParam = "page="
	// CreatedAfterParam that represents the date the query will look for candidaes
	CreatedAfterParam = "created_after="
	TokenURL          = ""
)
